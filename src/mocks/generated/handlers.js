/**
 * This file is AUTO GENERATED by [msw-auto-mock](https://github.com/zoubingwu/msw-auto-mock)
 * Feel free to commit/edit it as you need.
 */
/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import { HttpResponse, http } from "msw";
import { faker } from "@faker-js/faker";

faker.seed(1);

const baseURL = "";
const MAX_ARRAY_LENGTH = 20;
const MAX_STRING_LENGTH = 42;

// Map to store counters for each API endpoint
const apiCounters = new Map();

const next = (apiKey) => {
  let currentCount = apiCounters.get(apiKey) ?? 0;
  if (currentCount === Number.MAX_SAFE_INTEGER - 1) {
    currentCount = 0;
  }
  apiCounters.set(apiKey, currentCount + 1);
  return currentCount;
};

export const handlers = [
  http.delete(
    `${baseURL}/api/services/app/Role/Delete`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (
        shouldEchoRequestBody &&
        ["post", "put", "patch"].includes("delete")
      ) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]];

      const [body, init] =
        resultArray[
          next(`delete /api/services/app/Role/Delete`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.delete(
    `${baseURL}/api/services/app/Tenant/Delete`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (
        shouldEchoRequestBody &&
        ["post", "put", "patch"].includes("delete")
      ) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]];

      const [body, init] =
        resultArray[
          next(`delete /api/services/app/Tenant/Delete`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.delete(
    `${baseURL}/api/services/app/User/Delete`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (
        shouldEchoRequestBody &&
        ["post", "put", "patch"].includes("delete")
      ) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]];

      const [body, init] =
        resultArray[
          next(`delete /api/services/app/User/Delete`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.get(`${baseURL}/api/services/app/Role/Get`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getGetApiServicesAppRoleGet200Response(), { status: 200 }],
    ];

    const [body, init] =
      resultArray[next(`get /api/services/app/Role/Get`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/services/app/Role/GetAll`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getGetApiServicesAppRoleGetAll200Response(), { status: 200 }],
    ];

    const [body, init] =
      resultArray[
        next(`get /api/services/app/Role/GetAll`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(
    `${baseURL}/api/services/app/Role/GetAllPermissions`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getGetApiServicesAppRoleGetAllPermissions200Response(),
          { status: 200 },
        ],
      ];

      const [body, init] =
        resultArray[
          next(`get /api/services/app/Role/GetAllPermissions`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.get(
    `${baseURL}/api/services/app/Role/GetRoleForEdit`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getGetApiServicesAppRoleGetRoleForEdit200Response(),
          { status: 200 },
        ],
      ];

      const [body, init] =
        resultArray[
          next(`get /api/services/app/Role/GetRoleForEdit`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.get(`${baseURL}/api/services/app/Role/GetRoles`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getGetApiServicesAppRoleGetRoles200Response(), { status: 200 }],
    ];

    const [body, init] =
      resultArray[
        next(`get /api/services/app/Role/GetRoles`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(
    `${baseURL}/api/services/app/Session/GetCurrentLoginInformations`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getGetApiServicesAppSessionGetCurrentLoginInformations200Response(),
          { status: 200 },
        ],
      ];

      const [body, init] =
        resultArray[
          next(`get /api/services/app/Session/GetCurrentLoginInformations`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.get(`${baseURL}/api/services/app/Tenant/Get`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getGetApiServicesAppTenantGet200Response(), { status: 200 }],
    ];

    const [body, init] =
      resultArray[
        next(`get /api/services/app/Tenant/Get`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/services/app/Tenant/GetAll`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getGetApiServicesAppTenantGetAll200Response(), { status: 200 }],
    ];

    const [body, init] =
      resultArray[
        next(`get /api/services/app/Tenant/GetAll`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/services/app/User/Get`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getGetApiServicesAppUserGet200Response(), { status: 200 }],
    ];

    const [body, init] =
      resultArray[next(`get /api/services/app/User/Get`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/services/app/User/GetAll`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getGetApiServicesAppUserGetAll200Response(), { status: 200 }],
    ];

    const [body, init] =
      resultArray[
        next(`get /api/services/app/User/GetAll`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/services/app/User/GetRoles`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("get")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getGetApiServicesAppUserGetRoles200Response(), { status: 200 }],
    ];

    const [body, init] =
      resultArray[
        next(`get /api/services/app/User/GetRoles`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/api/services/app/Account/IsTenantAvailable`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getPostApiServicesAppAccountIsTenantAvailable200Response(),
          { status: 200 },
        ],
      ];

      const [body, init] =
        resultArray[
          next(`post /api/services/app/Account/IsTenantAvailable`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.post(
    `${baseURL}/api/services/app/Account/Register`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getPostApiServicesAppAccountRegister200Response(),
          { status: 200 },
        ],
      ];

      const [body, init] =
        resultArray[
          next(`post /api/services/app/Account/Register`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.post(
    `${baseURL}/api/services/app/Configuration/ChangeUiTheme`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]];

      const [body, init] =
        resultArray[
          next(`post /api/services/app/Configuration/ChangeUiTheme`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.post(`${baseURL}/api/services/app/Role/Create`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getPostApiServicesAppRoleCreate200Response(), { status: 200 }],
    ];

    const [body, init] =
      resultArray[
        next(`post /api/services/app/Role/Create`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/api/services/app/Tenant/Create`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getPostApiServicesAppTenantCreate200Response(), { status: 200 }],
      ];

      const [body, init] =
        resultArray[
          next(`post /api/services/app/Tenant/Create`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.post(
    `${baseURL}/api/services/app/User/Activate`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]];

      const [body, init] =
        resultArray[
          next(`post /api/services/app/User/Activate`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.post(
    `${baseURL}/api/services/app/User/ChangeLanguage`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]];

      const [body, init] =
        resultArray[
          next(`post /api/services/app/User/ChangeLanguage`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.post(
    `${baseURL}/api/services/app/User/ChangePassword`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getPostApiServicesAppUserChangePassword200Response(),
          { status: 200 },
        ],
      ];

      const [body, init] =
        resultArray[
          next(`post /api/services/app/User/ChangePassword`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.post(`${baseURL}/api/services/app/User/Create`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getPostApiServicesAppUserCreate200Response(), { status: 200 }],
    ];

    const [body, init] =
      resultArray[
        next(`post /api/services/app/User/Create`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/api/services/app/User/DeActivate`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]];

      const [body, init] =
        resultArray[
          next(`post /api/services/app/User/DeActivate`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.post(
    `${baseURL}/api/services/app/User/ResetPassword`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getPostApiServicesAppUserResetPassword200Response(),
          { status: 200 },
        ],
      ];

      const [body, init] =
        resultArray[
          next(`post /api/services/app/User/ResetPassword`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === "object" && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    },
  ),
  http.put(`${baseURL}/api/services/app/Role/Update`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("put")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getPutApiServicesAppRoleUpdate200Response(), { status: 200 }],
    ];

    const [body, init] =
      resultArray[
        next(`put /api/services/app/Role/Update`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.put(`${baseURL}/api/services/app/Tenant/Update`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("put")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getPutApiServicesAppTenantUpdate200Response(), { status: 200 }],
    ];

    const [body, init] =
      resultArray[
        next(`put /api/services/app/Tenant/Update`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.put(`${baseURL}/api/services/app/User/Update`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("put")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getPutApiServicesAppUserUpdate200Response(), { status: 200 }],
    ];

    const [body, init] =
      resultArray[
        next(`put /api/services/app/User/Update`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/TokenAuth/Authenticate`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ["post", "put", "patch"].includes("post")) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getPostApiTokenAuthAuthenticate200Response(), { status: 200 }],
    ];

    const [body, init] =
      resultArray[
        next(`post /api/TokenAuth/Authenticate`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === "object" && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
];

export function getPostApiServicesAppAccountIsTenantAvailable200Response() {
  return {
    state: faker.helpers.arrayElement([1, 2, 3]),
    tenantId: faker.number.int(),
  };
}

export function getPostApiServicesAppAccountRegister200Response() {
  return {
    canLogin: faker.datatype.boolean(),
  };
}

export function getPostApiServicesAppRoleCreate200Response() {
  return {
    id: faker.number.int(),
    name: faker.person.fullName(),
    displayName: faker.person.fullName(),
    normalizedName: faker.person.fullName(),
    description: faker.string.alpha({ length: { min: 0, max: 5000 } }),
    grantedPermissions: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
  };
}

export function getGetApiServicesAppRoleGetRoles200Response() {
  return {
    items: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.number.int(),
        name: faker.person.fullName(),
        displayName: faker.person.fullName(),
        isStatic: faker.datatype.boolean(),
        isDefault: faker.datatype.boolean(),
        creationTime: faker.date.anytime().toISOString(),
      }));
    })(),
  };
}

export function getPutApiServicesAppRoleUpdate200Response() {
  return {
    id: faker.number.int(),
    name: faker.person.fullName(),
    displayName: faker.person.fullName(),
    normalizedName: faker.person.fullName(),
    description: faker.string.alpha({ length: { min: 0, max: 5000 } }),
    grantedPermissions: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
  };
}

export function getGetApiServicesAppRoleGetAllPermissions200Response() {
  return {
    items: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.number.int(),
        name: faker.person.fullName(),
        displayName: faker.person.fullName(),
        description: faker.lorem.words(),
      }));
    })(),
  };
}

export function getGetApiServicesAppRoleGetRoleForEdit200Response() {
  return {
    role: {
      id: faker.number.int(),
      name: faker.person.fullName(),
      displayName: faker.person.fullName(),
      description: faker.string.alpha({ length: { min: 0, max: 5000 } }),
      isStatic: faker.datatype.boolean(),
    },
    permissions: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        name: faker.person.fullName(),
        displayName: faker.person.fullName(),
        description: faker.lorem.words(),
      }));
    })(),
    grantedPermissionNames: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
  };
}

export function getGetApiServicesAppRoleGet200Response() {
  return {
    id: faker.number.int(),
    name: faker.person.fullName(),
    displayName: faker.person.fullName(),
    normalizedName: faker.person.fullName(),
    description: faker.string.alpha({ length: { min: 0, max: 5000 } }),
    grantedPermissions: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
  };
}

export function getGetApiServicesAppRoleGetAll200Response() {
  return {
    items: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.number.int(),
        name: faker.person.fullName(),
        displayName: faker.person.fullName(),
        normalizedName: faker.person.fullName(),
        description: faker.string.alpha({ length: { min: 0, max: 5000 } }),
        grantedPermissions: (() => {
          const arrayMin = 1;
          const arrayMax = MAX_ARRAY_LENGTH;
          const safeMin = Math.min(arrayMin, arrayMax);
          return [
            ...new Array(
              faker.number.int({ min: safeMin, max: arrayMax }),
            ).keys(),
          ].map((_) => faker.lorem.words());
        })(),
      }));
    })(),
    totalCount: faker.number.int(),
  };
}

export function getGetApiServicesAppSessionGetCurrentLoginInformations200Response() {
  return {
    application: {
      version: faker.lorem.words(),
      releaseDate: faker.date.anytime().toISOString(),
      features: [...new Array(5).keys()]
        .map((_) => ({ [faker.lorem.word()]: faker.datatype.boolean() }))
        .reduce((acc, next) => Object.assign(acc, next), {}),
    },
    user: {
      id: faker.number.int(),
      name: faker.person.fullName(),
      surname: faker.person.fullName(),
      userName: faker.person.fullName(),
      emailAddress: faker.internet.email(),
    },
    tenant: {
      id: faker.number.int(),
      tenancyName: faker.person.fullName(),
      name: faker.person.fullName(),
    },
  };
}

export function getPostApiServicesAppTenantCreate200Response() {
  return {
    id: faker.number.int(),
    tenancyName: faker.person.fullName(),
    name: faker.person.fullName(),
    isActive: faker.datatype.boolean(),
  };
}

export function getGetApiServicesAppTenantGet200Response() {
  return {
    id: faker.number.int(),
    tenancyName: faker.person.fullName(),
    name: faker.person.fullName(),
    isActive: faker.datatype.boolean(),
  };
}

export function getGetApiServicesAppTenantGetAll200Response() {
  return {
    items: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.number.int(),
        tenancyName: faker.person.fullName(),
        name: faker.person.fullName(),
        isActive: faker.datatype.boolean(),
      }));
    })(),
    totalCount: faker.number.int(),
  };
}

export function getPutApiServicesAppTenantUpdate200Response() {
  return {
    id: faker.number.int(),
    tenancyName: faker.person.fullName(),
    name: faker.person.fullName(),
    isActive: faker.datatype.boolean(),
  };
}

export function getPostApiTokenAuthAuthenticate200Response() {
  return {
    accessToken: faker.lorem.words(),
    encryptedAccessToken: faker.lorem.words(),
    expireInSeconds: faker.number.int(),
    userId: faker.number.int(),
  };
}

export function getPostApiServicesAppUserCreate200Response() {
  return {
    id: faker.number.int(),
    userName: faker.person.fullName(),
    name: faker.person.fullName(),
    surname: faker.person.fullName(),
    emailAddress: faker.internet.email(),
    isActive: faker.datatype.boolean(),
    fullName: faker.person.fullName(),
    lastLoginTime: faker.date.anytime().toISOString(),
    creationTime: faker.date.anytime().toISOString(),
    roleNames: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
  };
}

export function getPutApiServicesAppUserUpdate200Response() {
  return {
    id: faker.number.int(),
    userName: faker.person.fullName(),
    name: faker.person.fullName(),
    surname: faker.person.fullName(),
    emailAddress: faker.internet.email(),
    isActive: faker.datatype.boolean(),
    fullName: faker.person.fullName(),
    lastLoginTime: faker.date.anytime().toISOString(),
    creationTime: faker.date.anytime().toISOString(),
    roleNames: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
  };
}

export function getGetApiServicesAppUserGetRoles200Response() {
  return {
    items: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.number.int(),
        name: faker.person.fullName(),
        displayName: faker.person.fullName(),
        normalizedName: faker.person.fullName(),
        description: faker.string.alpha({ length: { min: 0, max: 5000 } }),
        grantedPermissions: (() => {
          const arrayMin = 1;
          const arrayMax = MAX_ARRAY_LENGTH;
          const safeMin = Math.min(arrayMin, arrayMax);
          return [
            ...new Array(
              faker.number.int({ min: safeMin, max: arrayMax }),
            ).keys(),
          ].map((_) => faker.lorem.words());
        })(),
      }));
    })(),
  };
}

export function getPostApiServicesAppUserChangePassword200Response() {
  return faker.datatype.boolean();
}

export function getPostApiServicesAppUserResetPassword200Response() {
  return faker.datatype.boolean();
}

export function getGetApiServicesAppUserGet200Response() {
  return {
    id: faker.number.int(),
    userName: faker.person.fullName(),
    name: faker.person.fullName(),
    surname: faker.person.fullName(),
    emailAddress: faker.internet.email(),
    isActive: faker.datatype.boolean(),
    fullName: faker.person.fullName(),
    lastLoginTime: faker.date.anytime().toISOString(),
    creationTime: faker.date.anytime().toISOString(),
    roleNames: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
  };
}

export function getGetApiServicesAppUserGetAll200Response() {
  return {
    items: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.number.int(),
        userName: faker.person.fullName(),
        name: faker.person.fullName(),
        surname: faker.person.fullName(),
        emailAddress: faker.internet.email(),
        isActive: faker.datatype.boolean(),
        fullName: faker.person.fullName(),
        lastLoginTime: faker.date.anytime().toISOString(),
        creationTime: faker.date.anytime().toISOString(),
        roleNames: (() => {
          const arrayMin = 1;
          const arrayMax = MAX_ARRAY_LENGTH;
          const safeMin = Math.min(arrayMin, arrayMax);
          return [
            ...new Array(
              faker.number.int({ min: safeMin, max: arrayMax }),
            ).keys(),
          ].map((_) => faker.lorem.words());
        })(),
      }));
    })(),
    totalCount: faker.number.int(),
  };
}
